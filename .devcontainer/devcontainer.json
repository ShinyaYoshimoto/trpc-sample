{
  "image": "mcr.microsoft.com/devcontainers/javascript-node:22",
  "postStartCommand": "sudo corepack enable && corepack prepare pnpm@latest --activate && pnpm install && (setsid nohup pnpm dev > /tmp/dev.log 2>&1 < /dev/null &)"
}